Decorator Pattern（装饰者模式）
装饰者定义:
动态地将责任附加到对象上。若要扩展功能，装饰者提供了比继承更为弹性的方案

理解:
1.装饰者与被装饰的对象有相同的超类型（也就是父类）
2.可以用一个或者多个装饰者包装一个对象


父类的变量，子类可以直接使用

多重组合的时候，就可以用到装饰者模式
多重组和其实就是为了增强某项功能，例如cost功能。，所以必须重写cost方法，增强cost的性能。所以必须源于同一个抽象类，
重写同一个方法